name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  checks: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore BabyTracker.Tests/BabyTracker.Tests.csproj

      - name: Build
        run: dotnet build BabyTracker.Tests/BabyTracker.Tests.csproj --no-restore --configuration Release

      - name: Test
        run: dotnet test BabyTracker.Tests/BabyTracker.Tests.csproj --no-build --configuration Release --logger "trx;LogFileName=test-results.trx" --results-directory TestResults

      - name: Test Report
        uses: dorny/test-reporter@v3
        if: ${{ !cancelled() }}
        with:
          name: .NET Tests
          path: 'TestResults/**/*.trx'
          reporter: dotnet-trx