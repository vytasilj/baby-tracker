<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:loc="clr-namespace:BabyTracker.App.Localization"
             xmlns:vm="clr-namespace:BabyTracker.App.ViewModels"
             x:Class="BabyTracker.App.Views.MomSleepListPage"
             Title="{loc:Translate Key=MomSleep_Title}">
    <Grid RowDefinitions="*,Auto">
        <CollectionView Grid.Row="0"
                ItemsSource="{Binding Entries}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:MomSleepListItem">
                    <Grid Padding="15,10"
                            ColumnDefinitions="*,Auto">
                        <VerticalStackLayout Grid.Column="0"
                                Spacing="4">
                            <Label Text="{Binding Entry.StartTime, StringFormat='{0:d.M. HH:mm}'}"
                                    FontAttributes="Bold"/>
                            <Border IsVisible="{Binding IsInProgress}"
                                    BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                                    StrokeThickness="0"
                                    Padding="8,2"
                                    HorizontalOptions="Start">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10"/>
                                </Border.StrokeShape>
                                <Label Text="{Binding Duration}"
                                        FontSize="12"
                                       TextColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource PrimaryDarkText}}"/>
                            </Border>
                            <Label Text="{Binding Duration}"
                                    FontSize="13"
                                    Opacity="0.7"
                                    IsVisible="{Binding IsCompleted}"/>
                        </VerticalStackLayout>
                        <Button Grid.Column="1"
                                Text="✏️"
                                BackgroundColor="Transparent"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditEntryCommand}"
                                CommandParameter="{Binding .}"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Grid.Row="1"
                Text="{loc:Translate Key=Common_AddNew}"
                Margin="15"
                Command="{Binding AddNewCommand}"/>
    </Grid>
</ContentPage>